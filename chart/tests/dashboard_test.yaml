suite: 测试控制台暴露功能
templates:
  - dashboard.yaml
tests:
  - it: 默认关闭
    set:
      domain: example.com
    asserts:
      - notExists:
          path: '$.'
  - it: 打开后,默认生成一个入口
    set:
      domain: example.com
      dashboard:
        expose: true
    asserts:
      - matchSnapshot: { }