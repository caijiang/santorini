apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.santoriniConsoleBackendName" . }}
  labels:
    app.kubernetes.io/name: {{ include "common.santoriniConsoleBackendName" . }}
    {{- include "common.labels.withoutName" . | nindent 4 }}
spec:
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "common.santoriniConsoleBackendName" . }}
    {{- include "common.labels.withoutName" . | nindent 4 }}
