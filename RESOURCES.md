# 资源

资源是 santorini 集中管理的一些资源，隶属于环境可以被分配给服务。

## 资源类型

## 服务声明所需资源

是个数组

1. 类型
2. 可选的名字(如果存在多个同类型的 则必须声明)

## 资源配给器

最终生成一个唯一的 Secret 保存所有需要的环境变量。似乎不适合直接调用 api,通过服务端实现吧。
api 大致形式是 toEnv(resourceList,serviceType) 
